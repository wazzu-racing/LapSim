# GPS to track data

This is a tool to convert GPS data from the car datalogger to a set arcs of
constant radius, for use with the lap simulator.


## Usage
Make sure you have rust installed.

To build, run `cargo build`, and to run, `cargo run`. It will write the data to
`output/`, and write some plots as html files.
